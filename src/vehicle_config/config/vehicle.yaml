/**:
  ros__parameters:
    wheelbase: 0.33
    speed_to_erpm_gain: 4123.4
    speed_to_erpm_offset: 0.0

    steering_to_servo_gain: 1.2135
    steering_to_servo_offset: 0.5405

    max_steering_angle: 0.255

    servo_min: 0.25
    servo_max: 0.83

    max_speed: 8.0
    min_speed: -8.0

odometry_node:
  ros__parameters:
    speed_to_erpm_gain: 4123.4
    speed_to_erpm_offset: 0.0

    wheelbase: 0.325
    odom_frame: "odom"
    base_frame: "base_link"

    publish_tf: false
